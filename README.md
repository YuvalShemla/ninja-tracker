# Ninja Tracker

A mobile-first Ninja Warrior competition timer for recording obstacle attempts, rest periods, live standings, and course statistics.

## Run locally

```bash
npm install
npm run dev
```

Competition data is saved in the browser's local storage. Use the same browser and device for the full event.

## Deploy

Pushes to `main` are built and deployed automatically with GitHub Actions and GitHub Pages.
